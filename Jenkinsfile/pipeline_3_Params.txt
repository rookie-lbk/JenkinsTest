pipeline{
	agent any	//不分配节点 stage如果不指定 会报错 none 
	
	//parameters{
	//	string(name:"str",defaultValue:"defaultValue",description:"some words")
	//	
	//	text(name: 'DEPLOY_TEXT', defaultValue: 'One\nTwo\nThree\n', description: "--")
	//	
	//	booleanParam(name: 'DEBUG_BUILD', defaultValue: true, description: "--")
	//	
	//	choice(name:"choicevalueArr",choices:["valueC","ValueB","ValueA"],description:"this is a choice")
    //
	//	choice(name:"choicevalueArr2",choices:"valueA\nValueB\nValueC",description:"this is a choice")
    //
	//	
	//}
	
	
	
	stages{
		stage("empty test"){
			//agent {label "JenkinsTest"}
			steps{
				bat label:'',script:" echo helloworld"
				//echo "helloworld1"	//这里直接执行也是可以的
				//"svn help"
				// sleep(5)
				//bat label:'',script:" echo $str"
				
				
			}
		}
	}
	post{
		always{
			bat label:'',script:"echo always show this msg"
		}
		aborted{
			bat label:'',script:" echo some stage aborted"
		}
		failure{
			bat label:'',script:" echo some stage failure"
		}
		success{
			bat label:'',script:" echo all success"
		}
	}
	
}